{
  "name": "blackstone-breakaway",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "audit:data": "node tests/data-audit.mjs",
    "audit:unit": "node --test tests/unit/*.test.mjs",
    "audit:browser": "playwright test",
    "audit": "npm run audit:data && npm run audit:unit && npm run audit:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
